-- MANUAL ONLY. Back up and verify these tables before running.
-- Deploy the version without faculty editors/APIs first.
-- Never run automatically at application startup.
DROP TABLE IF EXISTS `faculty_history_events`, `faculty_history`, `faculty_pages`, `faculty_profiles`, `faculty_teachers`, `faculty_structure_images`, `faculty_structure_image`, `faculty_structure_nodes`, `faculty_vision_cards`, `faculty_vision_image`, `faculty_vision`;
